#include <stdio.h>
#include "dictionary.h"
#include "IOHandler.h"

int main() {
    /*
    Dictionary *dict = createDictionary();
    addPairToDict(dict, 2105, "Lada");
    addPairToDict(dict, 9, "abcdef");
    addPairToDict(dict, 1917, "Lenin");
    addPairToDict(dict, 52, "ALBLACK");
    printDictionary(dict);

    char *f = getDataByKey(dict, 9);
    printf("FOUND %s\n", f);

    clearPair(dict, 9);
    printDictionary(dict);
    */
   startInterfaceProcessing();
}